# MCP server default properties.
#
# The database connection (jdbc.url, jdbc.user, jdbc.password) is NOT defined here — it comes from
# VF_HOME/config/properties, the same as every other module.

# Maximum number of rows returned by read_query before the result is truncated.
vf.mcp.maxRows=1000
